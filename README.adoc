=== Distrobadges

Distribution version badges

.Devel Build:
[source, bash]
----
sudo apt-get install nim
nimble build
----

.Debian Build:
[source, bash]
----
sudo apt-get install nim
dpkg-buildpackage -us -uc -b
----
